title: CCS Specs
repository: icpc/ccs-specs
lang: en
description: CCS Specs

readme_index:
  with_frontmatter: true

theme: null

remote_theme: kattis/jekyll-rtd-theme

defaults:
  -
    scope:
      path: ""
    values:
      layout: "default-with-js"

exclude:
- Gemfile*
- update_docs.sh

plugins:
  - jekyll-remote-theme

markdown: CommonMarkGhPages
commonmark:
  options: ['SMART']
  extensions: ['table']
